# 🌙 موقع جدول ألعاب رمضان - Node.js بدون قاعدة بيانات

موقع كامل بـ **Node.js** - بدون حاجة لـ MongoDB!

## ✨ المميزات الجديدة

✅ **بدون قاعدة بيانات خارجية** - يستخدم ملفات JSON محلية  
✅ **تشغيل فوري** - لا تحتاج تثبيت MongoDB  
✅ **سهل جداً** - 3 خطوات فقط!  
✅ **البيانات محفوظة** - تُحفظ في ملفات JSON  

---

## 🚀 البدء السريع (3 خطوات)

### 1️⃣ تثبيت المكتبات
```bash
npm install
```

### 2️⃣ تشغيل الموقع
```bash
npm start
```

### 3️⃣ فتح المتصفح
```
http://localhost:3000
```

**خلاص! 🎉**

---

## 👤 إنشاء حساب Admin

### الطريقة 1: من الموقع + تعديل يدوي
1. افتح http://localhost:3000/login
2. سجل حساب جديد: admin / admin123
3. افتح ملف `data/users.json`
4. غيّر `"role": "user"` إلى `"role": "admin"`
5. احفظ الملف
6. سجل دخول من جديد

### الطريقة 2: باستخدام cURL
```bash
# سجل حساب
curl -X POST http://localhost:3000/register \
  -d "username=admin&password=admin123"

# بعدها غيّر الـ role في ملف data/users.json
```

---

## 📁 هيكل المشروع

```
ramadan-nodejs/
├── server.js              # الملف الرئيسي
├── package.json           # المكتبات
├── .env                   # الإعدادات
├── data/                  # 📂 ملفات البيانات (JSON)
│   ├── users.json        # المستخدمين
│   └── days.json         # الأيام
├── views/                 # صفحات HTML (EJS)
│   ├── index.ejs         # الصفحة الرئيسية
│   ├── login.ejs         # تسجيل الدخول
│   └── admin.ejs         # لوحة التحكم
└── public/               # الملفات الثابتة
    ├── css/
    │   └── style.css     # التصميم
    └── js/
        ├── main.js       # JS الرئيسية
        └── admin.js      # JS لوحة التحكم
```

---

## 💾 كيف تعمل البيانات؟

### ملفات JSON المحلية:
- **data/users.json** - يحفظ المستخدمين
- **data/days.json** - يحفظ جدول الأيام والتصويتات

### مثال على users.json:
```json
[
  {
    "id": "1708117234567",
    "username": "admin",
    "password": "$2a$10$...",
    "role": "admin",
    "createdAt": "2024-02-16T20:00:00.000Z"
  }
]
```

### مثال على days.json:
```json
[
  {
    "dayNumber": 1,
    "gameName": "FIFA",
    "time": "11:30 PM",
    "host": "أحمد",
    "notes": "يوم 1 من رمضان",
    "isSpecialEvent": false,
    "votes": [
      {
        "user": "محمد",
        "vote": "yes",
        "createdAt": "2024-02-16T20:00:00.000Z"
      }
    ],
    "isConfirmed": false
  }
]
```

---

## 🎯 المميزات

### ✅ للمستخدمين:
- تسجيل دخول وإنشاء حساب
- عرض جدول الألعاب (30 يوم)
- التصويت على الألعاب
- عداد تنازلي تفاعلي

### ✅ للأدمن:
- لوحة تحكم كاملة
- تعديل الأيام
- حذف الأيام
- إنشاء جدول 30 يوم تلقائي

### ✅ التصميم:
- تصميم رمضاني داكن
- Font Awesome Icons
- متجاوب 100%
- تأثيرات حركية

---

## 🔧 الإعدادات

### تغيير المنفذ
عدّل `.env`:
```env
PORT=3000
```

### تغيير مفاتيح الأمان
عدّل `.env`:
```env
JWT_SECRET=your_secret_here
SESSION_SECRET=your_session_secret_here
```

---

## 🔄 النسخ الاحتياطي

### حفظ البيانات:
```bash
# انسخ مجلد data كله
cp -r data data-backup-$(date +%Y%m%d)
```

### استرجاع البيانات:
```bash
# استرجع من النسخة الاحتياطية
cp -r data-backup-20240216/* data/
```

---

## 📦 النشر

### على Heroku:
```bash
# أضف Procfile
echo "web: node server.js" > Procfile

# أنشئ تطبيق
heroku create ramadan-games

# انشر
git push heroku main
```

### على Render / Railway:
1. رفع المشروع على GitHub
2. ربطه بـ Render/Railway
3. تعديل المتغيرات البيئية
4. خلاص! 🎉

---

## ⚡ الفرق عن النسخة السابقة

| الميزة | MongoDB | JSON محلي |
|--------|---------|-----------|
| تثبيت قاعدة بيانات | ❌ يحتاج MongoDB | ✅ لا يحتاج شيء |
| سهولة التشغيل | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| حفظ البيانات | ✅ قاعدة بيانات | ✅ ملفات JSON |
| السرعة | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| مناسب لـ | إنتاج كبير | مشاريع صغيرة/متوسطة |

---

## 🐛 حل المشاكل

### المشكلة: خطأ في npm install
```bash
rm -rf node_modules package-lock.json
npm install
```

### المشكلة: ملفات البيانات مفقودة
```bash
# أنشئها يدوياً
mkdir -p data
echo "[]" > data/users.json
echo "[]" > data/days.json
```

### المشكلة: نسيت كلمة مرور الأدمن
1. افتح `data/users.json`
2. احذف المستخدم القديم
3. سجل حساب جديد
4. غيّر الـ role لـ admin

---

## 📝 ملاحظات مهمة

⚠️ **للاستخدام الشخصي والمشاريع الصغيرة:**
- مناسب لـ 10-100 مستخدم
- البيانات محفوظة في ملفات محلية
- سهل النسخ الاحتياطي

💡 **للمشاريع الكبيرة:**
- استخدم النسخة مع MongoDB
- أفضل للأداء العالي
- يدعم آلاف المستخدمين

---

## 🌟 التطوير المستقبلي

- [ ] إضافة SQLite بدلاً من JSON
- [ ] نظام نسخ احتياطي تلقائي
- [ ] تصدير/استيراد البيانات
- [ ] API للتطبيقات الخارجية

---

**رمضان كريم!** 🌙✨

صُنع بـ ❤️ بالكامل في Node.js - بدون قواعد بيانات معقدة!
